<!--/workspaces/Inventory-Management-System/app/views/order_products/_form.html.erb-->
<%= form_with(model: order_item) do |form| %>
  <% if order_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(order_item.errors.count, "error") %> prohibited this order_item from being saved:</h2>

      <ul>
        <% order_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :quantity_ordered, style: "display: block" %>
    <%= form.text_field :quantity_ordered %>
  </div>

  <div>
    <%= form.label :unit_price_id, style: "display: block" %>
    <%= form.text_field :unit_price_id %>
  </div>

  <div>
    <%= form.label :shipping_cost_id, style: "display: block" %>
    <%= form.text_field :shipping_cost_id %>
  </div>

  <%= form.hidden_field :company_id, value: current_user.company_id %>
  <%= form.hidden_field :branch_id, value: current_user.branch_id %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
